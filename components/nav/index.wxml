<view class="container">
  <image bind:tap="onLeft" class="icon" src="{{ last ? disLeft : left}}"/>
  <text>{{title}}</text>
  <image bind:tap="onRight"  class="icon" src="{{first ? disRight : right}}" />
</view>